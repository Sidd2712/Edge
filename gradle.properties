# Enable the modern AndroidX libraries
android.useAndroidX=true

# Automatically convert old libraries to work with AndroidX
android.enableJetifier=true

# Optional: Gives Gradle more memory for faster builds on the cloud
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8